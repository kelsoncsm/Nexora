import{Component,inject,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{HttpClient}from'@angular/common/http';import{APP_CONFIG}from'../../core/config/app-config';
interface Customer{id:string;name:string;phone?:string;email?:string;notes?:string}interface Page{items:Customer[];total:number}
@Component({selector:'app-customer-page',imports:[FormsModule],template:`<main><header><div><small>Tenant</small><h1>Clientes</h1></div><button (click)="newCustomer()">Novo cliente</button></header><input [(ngModel)]="search" (keyup.enter)="load()" placeholder="Buscar por nome, telefone ou e-mail">@if(editing()){<form (ngSubmit)="save()"><input [(ngModel)]="form.name" name="name" required placeholder="Nome"><input [(ngModel)]="form.phone" name="phone" placeholder="Telefone"><input [(ngModel)]="form.email" name="email" placeholder="E-mail"><textarea [(ngModel)]="form.notes" name="notes" placeholder="Observações"></textarea><button>Salvar</button><button type="button" (click)="editing.set(false)">Cancelar</button></form>}<section>@for(c of customers();track c.id){<article><div><b>{{c.name}}</b><span>{{c.email||c.phone||'Sem contato'}}</span></div><button (click)="edit(c)">Editar</button><button (click)="remove(c.id)">Excluir</button></article>}@empty{<p>Nenhum cliente encontrado.</p>}</section></main>`,styles:[`:host{display:block;min-height:100vh;background:#f5f7fb}main{max-width:960px;margin:auto;padding:2rem}header,article{display:flex;justify-content:space-between;align-items:center;gap:1rem}h1{margin:.2rem 0 1rem}input,textarea{padding:.8rem;border:1px solid #ccd5e3;border-radius:.5rem}main>input{width:min(100%,500px)}form{display:grid;gap:.7rem;background:white;padding:1rem;margin:1rem 0;border-radius:.8rem}article{background:white;padding:1rem;margin:.6rem 0;border-radius:.7rem}article div{display:flex;flex-direction:column}button{padding:.6rem .9rem;border:0;border-radius:.5rem;background:#17345d;color:white}@media(max-width:600px){article{align-items:flex-start;flex-wrap:wrap}}`]})
export class CustomerPage{private http=inject(HttpClient);private cfg=inject(APP_CONFIG);customers=signal<Customer[]>([]);editing=signal(false);search='';form:any={};constructor(){this.load()}load(){this.http.get<Page>(`${this.cfg.apiBaseUrl}/customers?search=${encodeURIComponent(this.search)}`).subscribe(x=>this.customers.set(x.items))}newCustomer(){this.form={name:'',phone:'',email:'',notes:''};this.editing.set(true)}edit(c:Customer){this.form={...c};this.editing.set(true)}save(){const req=this.form.id?this.http.put(`${this.cfg.apiBaseUrl}/customers/${this.form.id}`,this.form):this.http.post(`${this.cfg.apiBaseUrl}/customers`,this.form);req.subscribe(()=>{this.editing.set(false);this.load()})}remove(id:string){this.http.delete(`${this.cfg.apiBaseUrl}/customers/${id}`).subscribe(()=>this.load())}}
